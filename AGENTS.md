# AGENTS.md – Swap&Rent Project

This file used to be a full copy of the project instructions and had drifted
out of sync with the maintained version — missing sections like the SAS /
Badge Jump design vocabulary that `CLAUDE.md` picked up later.

**`CLAUDE.md` (repo root) is the single source of truth for project
instructions.** Read that file instead of duplicating its content here.
